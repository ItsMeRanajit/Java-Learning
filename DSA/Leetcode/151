class Solution {
    public String reverseWords(String s) {
        String str[] = s.split("\\s+");
        StringBuilder str2 = new StringBuilder();
        for(int i = str.length - 1 ; i >= 0 ; i--)  str2.append(str[i]).append(' ');
        return str2.toString().trim();
    }
}