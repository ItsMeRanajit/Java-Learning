class Solution {
    public String firstPalindrome(String[] words) {
        for(String i : words){
            StringBuilder str = new StringBuilder(i);
            if(str.reverse().toString().equals(i))   return i;
        }
        return "";
    }
}